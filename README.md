# Apple Responsive Replica

This project is a high-fidelity, responsive clone of the Apple homepage, designed to demonstrate modern front-end layout techniques using Bootstrap and custom CSS. It recreates the sleek, minimalist aesthetic characteristic of Apple’s web presence.

---

## 🚀 Features

- **Responsive Design:** Utilizes Bootstrap’s grid system and custom media queries to ensure a seamless experience across mobile, tablet, and desktop devices.  
- **Sticky Navigation:** A semi-transparent navigation bar that remains fixed at the top of the viewport while scrolling.  
- **Interactive Footer:** Features a complex multi-column layout that collapses into an accordion-style interface on smaller screens.  
- **Icon Integration:** Uses FontAwesome and custom image assets for navigation and product logos.  
- **Typography:** Implements the "Montserrat" font family from Google Fonts to emulate a premium UI feel.  

---

## 🛠️ Built With

- **HTML5:** Semantic structure for product sections and footer.  
- **CSS3:** Custom styling for background images, hover effects, and typography.  
- **Bootstrap (v4):** Used for the responsive grid, navigation toggles, and utility classes.  
- **jQuery:** Powering the Bootstrap JavaScript components.  

---

## 📝 Note on Assets

The project references local image paths (e.g., `images/home/mac-laptop.jpg`) and external CDNs for FontAwesome and Google Fonts. Ensure the `images` directory is present in your local environment for the visuals to render correctly.

